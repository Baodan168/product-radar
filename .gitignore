__pycache__/
*.pyc
.venv/
logs/
*.log

# Auto-generated scan data (pushed via API, not version controlled)
data/channels/
data/discovery/
data/history/

# Large cached data
data/amazon_cache.json

# Output files — explicitly tracked via API, not git
output/*
!output/index.html
!output/platform.html

# Visual regression baselines (本地回归辅助，重构期间用，不入库)
.screenshots/

# Claude Code 的个人覆盖配置（.claude/settings.json 入库，local 不入库）
.claude/settings.local.json

# Internal team docs — not for public repo
OA系统介绍-团队版.md
OA系统介绍-对外版.md
bitable_sync.py

